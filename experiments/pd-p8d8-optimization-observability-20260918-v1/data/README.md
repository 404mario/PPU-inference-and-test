# 数据说明

`results.json` 是实验的可公开观测数据：候选级别只保留计数、端到端 makespan、有效输出 token/s、P/D 阶段聚合时间和 P 端到达/分波指标。它不含请求或响应内容、标识符、绝对时间、网络与机器信息。

`configuration.json` 只保留影响可比性的部署参数和候选间唯一差异。`provenance.json` 保存来源散列及脱敏规则。所有性能时间单位为秒，除非字段名以 `_ms` 结尾。

逐轮原始时间序列、请求与响应、日志和原始 metrics 保留在私有归档；公开版本明确标出这一边界，避免它们被误认为完整可复现的运行转储。
